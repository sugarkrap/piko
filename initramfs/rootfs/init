#!/bin/sh
mount -t proc proc /proc
mount -t sysfs sysfs /sys
mount -t devtmpfs devtmpfs /dev 2>/dev/null

echo ""
echo "=== zaurus-refresh minimal initramfs, corgi kernel smoke test ==="
echo ""

exec /bin/sh
